# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

def install_pods
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for hololive
  pod 'URLImage'
  pod 'Alamofire', '~> 5.2'
  pod 'FontAwesome.swift'
end

target "hololive" do
  install_pods
end

target "hololiveWidgetExtension" do
  install_pods
end
